import React from 'react';

const LayoutFilter = ({category,setCategoty}) => {
    return (
        <div>
            <form>
                <select name="" id="">
                    <option>
                        
                    </option>
                </select>
            </form>
            
        </div>
    );
};

export default LayoutFilter;

<button onClick={() => dispatch(filterProductsthunk(categor.id))}>{categor.name}</button>